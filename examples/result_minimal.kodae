' v1 catch-style sample (no result[T], ok/err, or ?)
fn read_file(path: str) -> str {
  return path
}

fn main() {
  let save_data = read_file("save.dat") catch (err) {
    print("Failed: " + err)
  }
  print("Loaded: " + save_data)
}
