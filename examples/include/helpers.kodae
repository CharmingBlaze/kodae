' Shared by main.kodae via  #include "helpers"
' Symbols used from another file are exported with pub.

fn triple(n: int) -> int {
  return n * 3
}

struct Point {
  x: int
  y: int
}

fn point_sum(p: Point) -> int {
  return p.x + p.y
}
