' Shared by main.clio via  #include "helpers"
' Symbols used from another file are exported with pub.

pub fn triple(n: int) -> int {
  return n * 3
}

pub struct Point {
  x: int
  y: int
}

pub fn point_sum(p: Point) -> int {
  return p.x + p.y
}
